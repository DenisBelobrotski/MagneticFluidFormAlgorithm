#pragma once

#include <vector>

namespace algorithm
{
    void calcRightSweep(
            std::vector<std::vector<double>> &matrix, std::vector<double> &freeMembers, std::vector<double> &result);
}
